# Server
PORT=3001

# Database (PostgreSQL — use Railway's PostgreSQL plugin values)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=16102000
DB_DATABASE=ecommerce-db
DB_SYNCHRONIZE=true

# Auth
JWT_SECRET=super-secret-key

# App
BASE_URL=http://localhost:3001
